我现在要对系统进行重构 主要目的 1.让AI输出有严谨结构的的数据结构 便于前端展示和打印 2.前端固定一套代码 按照题目类型进行渲染 2。后端按照既定逻辑清晰数据，校验AI数据，添加必要字段，如：每个题目需要控制预留行数来作答 后端按照既定逻辑填充的新字段

我的初步技术方案 
1.后端拿到AI会烦的数据做清洗，检验json使用 给出完整的 JSON Schema（json-schema）文件以便后端校验
2.前端每个题型都有对应的component渲染

要求
1.你的修改新增代码 不要影响老代码 老逻辑 开发完整直接替换
2.可以修改 frontend\src\HistoryDetail.tsx代码 我来检验你的前端代码是否正确  
3.AI输出结果案例可以查看 document\ai_resp.txt

【题型分类 & 字段规则】
1. SINGLE_CHOICE：单选题，四选一，必须加 options 数组（顺序对应 A/B/C/D）。（只能存在于 语文，数学，英语）
2. MULTIPLE_CHOICE：多选题，必须加 options 数组。（只能存在于 语文，数学，英语）
3. TRUE_FALSE：判断题 （只能存在于 语文，数学，英语）
4. FILL_BLANK：填空题，**题干中用连续3个下划线 ___ 表示挖空**。（只能存在于 语文，数学，英语）
5. CALCULATION：数学计算题/解方程/脱式计算。（只能存在于 数学）
6. WORD_PROBLEM：数学应用题，有实际场景。（只能存在于 数学）
7. GEOMETRY：数学几何题，涉及图形。（只能存在于 数学）
8. READ_COMP：语文/英语阅读理解，有阅读材料，必须加 sub_questions 数组（子题结构同单题）。（只能存在于 语文，英语）
9. POETRY_APP：语文古诗文鉴赏/默写，或英语的默写解答（只能存在于 语文，英语）
10. CLOZE：英语完形填空。（只能存在于 英语）
11. ESSAY：语文作文/英语书面表达。 （只能存在于 语文，英语）
